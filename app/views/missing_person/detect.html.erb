<h3>Search Results...</h3>

<%= image_tag @person.images.first.image.url %>

<% unless @result.empty? %>
  We think you might look like the following people...

  <% @result.each do |user_id, data| %>
    <img src=<%= data["url"] %> />
    (<%= data["confidence"] %> % sure)
  <% end %>
<% else %>
  No matches found sorry
<% end %>